/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WizardFooterRefDirective } from './wizard-footer-ref.directive';

describe('Directive: WizardFooterRef', () => {
  it('should create an instance', () => {
    const directive = new WizardFooterRefDirective();
    expect(directive).toBeTruthy();
  });
});
